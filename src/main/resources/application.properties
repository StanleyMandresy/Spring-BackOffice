# ===============================
# LOCAL ENV
# ===============================

# Database
db.url=jdbc:postgresql://localhost:5432/bo_local
db.username=bo_user
db.password=bo_pass
db.driver=org.postgresql.Driver

# Hibernate
hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
hibernate.show_sql=true
hibernate.hbm2ddl.auto=validate

# Application
app.env=local
