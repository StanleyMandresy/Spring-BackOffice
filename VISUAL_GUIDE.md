╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                           ║
║     🔐 API D'AUTHENTIFICATION JSON - BACKOFFICE                          ║
║     Framework Maison + Spring JDBC + Sprint 11 bis                       ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│  📦 NOUVEAU CONTRÔLEUR: ApiAuthController.java                          │
└─────────────────────────────────────────────────────────────────────────┘

  Endpoints Publics (@AllowAnonymous):
  ────────────────────────────────────────
  POST   /api/auth/login       → Connexion avec session
  POST   /api/auth/register    → Inscription nouvel utilisateur
  POST   /api/auth/logout      → Déconnexion
  GET    /api/auth/check       → Vérifier statut auth

  Endpoints Protégés (@RequireAuth):
  ────────────────────────────────────────
  GET    /api/auth/me          → Profil utilisateur connecté


┌─────────────────────────────────────────────────────────────────────────┐
│  🚀 DÉMARRAGE RAPIDE                                                    │
└─────────────────────────────────────────────────────────────────────────┘

  1️⃣  Compiler et déployer:
      ./deploy-and-test.sh

  2️⃣  Tester l'API:
      ./test-api.sh

  3️⃣  Test manuel rapide:
      curl "http://localhost:8080/sprint0/api/auth/check"


┌─────────────────────────────────────────────────────────────────────────┐
│  📋 EXEMPLE DE FLUX D'AUTHENTIFICATION                                  │
└─────────────────────────────────────────────────────────────────────────┘

  ┌──────────────┐
  │   Client     │
  └──────┬───────┘
         │
         │ POST /api/auth/login
         │ username=admin&password=adminpass
         │
         v
  ┌──────────────────────────────────────────┐
  │  ApiAuthController                       │
  │  ┌────────────────────────────────────┐  │
  │  │ 1. Authentifier avec JdbcTemplate  │  │
  │  │ 2. User.authenticate(jdbc, u, p)   │  │
  │  │ 3. Créer session HTTP               │  │
  │  │ 4. session.setAttribute("user", u)  │  │
  │  │ 5. Retourner JsonResponse          │  │
  │  └────────────────────────────────────┘  │
  └──────┬───────────────────────────────────┘
         │
         │ JSON Response + Cookie JSESSIONID
         │ {"status":"success", "data":{...}}
         │
         v
  ┌──────────────┐
  │   Client     │ 
  │ (avec cookie)│
  └──────┬───────┘
         │
         │ GET /api/auth/me
         │ Cookie: JSESSIONID=xxx
         │
         v
  ┌──────────────────────────────────────────┐
  │  @RequireAuth vérifie session            │
  │  ┌────────────────────────────────────┐  │
  │  │ 1. FrontServlet vérifie @RequireAuth│ │
  │  │ 2. Cherche "user" dans session      │  │
  │  │ 3. Si présent → OK                  │  │
  │  │ 4. Si absent → 403 Forbidden        │  │
  │  │ 5. Injecte @Session params          │  │
  │  │ 6. Retourne JsonResponse            │  │
  │  └────────────────────────────────────┘  │
  └──────┬───────────────────────────────────┘
         │
         │ JSON Response
         │ {"status":"success", "data":{...}}
         │
         v
  ┌──────────────┐
  │   Client     │
  └──────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│  🎯 EXEMPLES DE RÉPONSES JSON                                           │
└─────────────────────────────────────────────────────────────────────────┘

  ✅ Succès (Login):
  ──────────────────────────────────────────────────────────────────────
  {
    "status": "success",
    "code": 200,
    "data": {
      "id": 1,
      "username": "admin",
      "role": "USER",
      "message": "Connexion réussie"
    }
  }

  ❌ Erreur (Identifiants incorrects):
  ──────────────────────────────────────────────────────────────────────
  {
    "status": "error",
    "code": 401,
    "data": {
      "error": "Identifiants incorrects"
    }
  }

  🔒 Erreur (Non authentifié):
  ──────────────────────────────────────────────────────────────────────
  {
    "status": "error",
    "code": 403,
    "data": {
      "error": "Accès non autorisé"
    }
  }


┌─────────────────────────────────────────────────────────────────────────┐
│  🛠️  ARCHITECTURE TECHNIQUE                                             │
└─────────────────────────────────────────────────────────────────────────┘

  Framework Maison (Sprint 11 bis):
  ═══════════════════════════════════════════════════════════════════════
  @Controller          → Déclarer un contrôleur
  @RestAPI             → Retour JSON automatique (JsonSerializer)
  @PostMapping("/url") → Mapper une route POST
  @GetMapping("/url")  → Mapper une route GET
  @RequestParam("x")   → Injecter paramètre de requête
  @Session("key")      → Injecter valeur de session
  @RequireAuth         → Protéger route (auth requise)
  @AllowAnonymous      → Autoriser accès public

  Spring Framework:
  ═══════════════════════════════════════════════════════════════════════
  JdbcTemplate         → Requêtes SQL simplifiées
  DataSource           → Connexion PostgreSQL
  ApplicationContext   → Conteneur Spring

  Jakarta Servlet:
  ═══════════════════════════════════════════════════════════════════════
  HttpSession          → Gestion session HTTP
  Cookie JSESSIONID    → Identifiant session


┌─────────────────────────────────────────────────────────────────────────┐
│  📁 FICHIERS CRÉÉS                                                      │
└─────────────────────────────────────────────────────────────────────────┘

  Code Source:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✨ src/main/java/com/spring/BackOffice/controller/
     └── ApiAuthController.java  (200+ lignes)

  Documentation:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  📚 README_API.md               → Documentation complète (300+ lignes)
  🧪 TEST_CURL.md                → Guide tests curl (200+ lignes)
  ⚡ QUICK_TESTS.md              → Tests rapides (150+ lignes)
  📊 SUMMARY.md                  → Résumé des modifications
  📋 VISUAL_GUIDE.md             → Ce fichier (guide visuel)

  Scripts:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🚀 deploy-and-test.sh          → Déploiement automatisé
  🧪 test-api.sh                 → Suite de tests complète

  Outils:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  📮 BackOffice_Auth_API.postman_collection.json  → Collection Postman


┌─────────────────────────────────────────────────────────────────────────┐
│  🧪 TESTS RAPIDES                                                       │
└─────────────────────────────────────────────────────────────────────────┘

  Test 1: Vérifier que l'API fonctionne
  ──────────────────────────────────────────────────────────────────────
  $ curl "http://localhost:8080/sprint0/api/auth/check"

  Test 2: Se connecter
  ──────────────────────────────────────────────────────────────────────
  $ curl -X POST "http://localhost:8080/sprint0/api/auth/login" \
         -d "username=admin&password=adminpass" \
         -c cookies.txt

  Test 3: Voir son profil
  ──────────────────────────────────────────────────────────────────────
  $ curl "http://localhost:8080/sprint0/api/auth/me" -b cookies.txt

  Test 4: Tous les tests automatiques
  ──────────────────────────────────────────────────────────────────────
  $ ./test-api.sh


┌─────────────────────────────────────────────────────────────────────────┐
│  ✅ CHECKLIST AVANT DE TESTER                                          │
└─────────────────────────────────────────────────────────────────────────┘

  [ ] PostgreSQL est démarré
  [ ] Base de données "sprint0" existe
  [ ] Table "users" existe avec données de test
  [ ] Credentials DB corrects dans application.properties
  [ ] Framework jar (front-servlet-app) est installé
  [ ] Tomcat est démarré
  [ ] Application déployée (sprint0.war dans webapps)


┌─────────────────────────────────────────────────────────────────────────┐
│  🎓 CE QUE VOUS AVEZ APPRIS                                            │
└─────────────────────────────────────────────────────────────────────────┘

  ✅ Créer une API RESTful avec retour JSON
  ✅ Gérer l'authentification par session HTTP
  ✅ Utiliser les annotations du framework maison (Sprint 11 bis)
  ✅ Protéger des routes avec @RequireAuth
  ✅ Injecter des valeurs de session avec @Session
  ✅ Tester une API avec curl
  ✅ Gérer les erreurs HTTP (401, 403, 409, 500)
  ✅ Documenter une API
  ✅ Automatiser les tests


┌─────────────────────────────────────────────────────────────────────────┐
│  💡 CONSEILS                                                            │
└─────────────────────────────────────────────────────────────────────────┘

  • Toujours utiliser -c cookies.txt lors du login
  • Toujours utiliser -b cookies.txt pour les requêtes suivantes
  • Vérifier les logs Tomcat en cas d'erreur 500
  • Utiliser -v pour voir les headers en cas de problème
  • Consulter TEST_CURL.md pour la doc complète


┌─────────────────────────────────────────────────────────────────────────┐
│  🔗 RESSOURCES                                                          │
└─────────────────────────────────────────────────────────────────────────┘

  Documentation:
  • README_API.md        - Documentation API complète
  • TEST_CURL.md         - Guide des tests curl
  • QUICK_TESTS.md       - Tests rapides copy-paste
  • SUMMARY.md           - Résumé des modifications

  Framework:
  • Framework-S5/framework/README.md              - Doc framework
  • Framework-S5/test/.../sprint11bis-security.html - Exemples Sprint 11 bis


╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                           ║
║  🎉 API PRÊTE À L'EMPLOI !                                               ║
║                                                                           ║
║  Pour commencer:  ./deploy-and-test.sh                                   ║
║  Pour tester:     ./test-api.sh                                          ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝
